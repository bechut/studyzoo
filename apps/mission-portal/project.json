{
  "name": "mission-portal",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mission-portal/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/mission-portal"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "mission-portal:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "mission-portal:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "mission-portal:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "mission-portal:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "mission-portal:build:development"
        },
        "production": {
          "buildTarget": "mission-portal:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["coverage/apps/mission-portal"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/mission-portal"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/mission-portal/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "serve-static": {
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "mission-portal:build"
      }
    }
  },
  "tags": []
}
